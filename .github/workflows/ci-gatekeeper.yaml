name: CI Gatekeeper
on:
  workflow_run:
    workflows: ["Backend tests"]
    types:
      - completed
jobs:
  check-all-passed:
    runs-on: ubuntu-latest
    steps:
      - name: Verify all required workflows passed
        run: |
          echo "All required workflows passed. Merge allowed."